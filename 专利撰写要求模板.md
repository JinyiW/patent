# 专利交底书撰写规范

## 1. 适用范围
- 适用于所有新增专利，不限技术领域。
- 每件专利独立成文，不混写其他专利主题。
- 最终输出基于 `发明专利技术交底书模板.docx`，由 `fill_template.py` 自动填充。

## 2. 章节结构（必须严格匹配 Word 模板）

disclosure.md 中必须包含以下章节，Markdown 标题层级与模板对应关系如下：

| # | Markdown 标题 | 对应模板位置 |
|---|--------------|-------------|
| 1 | `## 交底书名称` | 表格：交底书名称 |
| 2 | `## 缩略语和关键术语定义` | 1、【关键术语】 |
| 3 | `## 1、*本发明的技术关键点（欲保护点）` | 2、【发明构思】 |
| 4 | `## 2、*与本发明最相近的现有技术` | 3、【背景技术】（标题行） |
| 5 | `### 2.1 现有技术的技术方案` | 3.1 相关背景描述 |
| 6 | `### 2.2 现有技术缺点及本发明解决的问题` | 3.2 现有技术的缺点 |
| 7 | `## 3、*本发明技术方案的详细阐述` | 4、【发明内容】（标题行） |
| 8 | `### 3.1 产品侧` | 4.1 产品侧 |
| 9 | `### 3.2 技术侧` | 4.2 技术侧 |
| 10 | `## 4、*技术方案所产生的有益效果` | 4.3 有益效果 |
| 11 | `## 5、发散思维，针对3中的技术方案，是否还有其他别的替代方案` | （合并到4.3末尾） |
| 12 | `## 附件参考文献（如：专利/论文/网页/期刊）` | 5、参考文献 |

> 注意：Markdown 中的编号（1~5）与 Word 模板中的编号（1~5）存在偏移映射，这是 `fill_template.py` 自动处理的，撰写时按上表左列编号即可。

## 3. 字数与内容要求
- 目标篇幅：约 5000 字（正文口径，不含参考文献链接）。
- 控制区间：4800–5600 字。
- 内容要覆盖：技术问题、核心创新点、输入输出、方法流程、约束条件、量化效果、替代方案。

## 4. 公式规范
- 行内公式：`\(...\)` —— 如 `任务集合 \(\mathcal{I}\)`
- 块级公式：`$$...$$`（单独成行）—— 如：
  ```
  $$
  t_{i,j} = \frac{w_i}{e_{j,k(i),s(i)}} \cdot \gamma_{i,j}
  $$
  ```
- `fill_template.py` 会自动将 LaTeX 转为 Word 原生 OMML 可编辑公式。
- 公式转换失败时会 fallback 为纯文本，撰写后建议用工具验证公式是否正确渲染。

## 5. 插图规范
- 图片放在专利目录下的 `figures/` 子目录中。
- 命名规则：`fig_<序号>_<描述>.png`，如 `fig_1_system_architecture_0.png`。
- 可通过 `gen_figures.py` 根据 `brief.md` 中的图片需求自动生成。
- **图片标题必须用中文**：在 `figures/captions.txt` 中为每张图片指定中文标题（一行一条）：
  ```
  fig_1_system_architecture_0.png  图1 系统架构示意图
  fig_2_method_flowchart_0.png  图2 方法流程图
  fig_3_bipartite_matching_0.png  图3 任务匹配示意图
  ```
  如果没有 `captions.txt`，工具会使用通用的"图N 技术示意图"作为标题。**不要依赖英文文件名自动生成标题。**
- **图片必须分散插入**：`fill_template.py` 会将图片均匀分布到 4.1 产品侧和 4.2 技术侧的正文之间，实现图文交叉。不要把所有图片集中在一处。
- 图片风格：黑白线条技术示意图，中文标注，专利规范。

## 6. 合规与匿名化要求
- 不出现个人姓名、手机号、邮箱、部门名称等信息。
- 交底书名称中不出现公司或产品名。
- 撰写人信息由提交者自行在 Word 模板中填写，工具不自动填入。

## 7. 文件组织

```
patents/patent_<NN>_<主题>_<日期>/
├── brief.md                       # 技术素材（可选，用于AI辅助撰写和图片生成）
├── patent_<NN>_disclosure.md      # 交底书 Markdown 源文件
├── patent_<NN>_disclosure.docx    # Word 交付版（由 fill_template.py 生成）
└── figures/                       # 插图
    ├── fig_1_xxx.png
    └── fig_2_xxx.png
```

## 8. 生成流程

```bash
# 1. 根据 brief.md 生成插图（可选）
python tools/gen_figures.py patent_<NN>

# 2. 将 disclosure.md 填充到交底书模板，生成最终 DOCX
python tools/fill_template.py patent_<NN>
```

## 9. 交付前自检清单
1. 章节是否完全匹配上述 12 项（含 2.1/2.2/3.1/3.2）。
2. 是否仅围绕当前专利主题，无跨主题混写。
3. 3.1 产品侧是否有应用场景与产品落地方案。
4. 3.2 技术侧是否具备可执行技术细节（输入/输出/算法/约束/流程步骤）。
5. 技术关键点（第1节）与替代方案（第5节）是否能支撑权利要求扩展。
6. 正文字数是否在 4800–5600 区间。
7. 公式是否使用 `\(...\)` / `$$...$$` 格式，避免 `$...$` 单美元号。
8. 是否完成匿名化（无实名与联系方式）。
9. `figures/captions.txt` 是否存在且所有标题均为中文（不允许英文标题）。
10. 生成 DOCX 后检查图片是否均匀分散在正文中（不允许集中在一处）。
11. 参考文献是否可访问且与正文相关。
