#!/usr/bin/env python3
"""批量生成 17 张专利技术插图"""
import sys, time
sys.path.insert(0, '/data/zhuanli/tools')
from remote_image_generator import generate_images_with_remote_api

OUTPUT_DIR = '/data/zhuanli/output/imagegen/patent_figures'

# (prefix, prompt) 列表
FIGURES = [
    # ── 专利1 ──
    ("fig_1_2_sparse_vs_dense", """专利技术插图，黑白线条图，白色背景，对比示意图风格。

左右两部分对比布局：

【左侧 - 稠密雅可比矩阵】：
- 绘制一个大的矩形矩阵网格（约8×8格）
- 所有网格内用斜线填充或灰色填充，表示全部非零
- 矩阵上方标注「稠密矩阵 J_dense」
- 矩阵下方标注「规模：O(N×B)」「N=全部顶点，B=全部骨骼」
- 旁边画一个大的内存条图标，标注「高内存占用」

【右侧 - 稀疏雅可比矩阵】：
- 绘制同样大小的矩形矩阵网格
- 只有少数分散的对角块区域用斜线填充，其余为空白
- 矩阵上方标注「稀疏矩阵 J_sparse」
- 矩阵下方标注「规模：O(Na×K)」「Na=受影响顶点，K=候选骨骼」
- 旁边画一个小的内存条图标，标注「低内存占用」

中间用「VS」或双向箭头分隔

底部对比说明：
「非零元素：100%」←→「非零元素：<5%」"""),

    ("fig_1_3_vertex_bone_subspace", """专利技术插图，黑白线条图，白色背景，三维网格示意图风格。

绘制一个简化的人体上半身网格轮廓（肩膀到手臂区域），用细线条表示网格边：

【网格部分】：
- 大部分网格顶点用小空心圆点表示（正常顶点）
- 肩关节附近一片区域的顶点用实心黑点表示，并用虚线圈起来，标注「受影响顶点集 Va」
- 该区域用浅灰色底纹标记

【骨架部分】：
- 在网格内部绘制简化骨架：上臂骨、前臂骨、肩骨、锁骨
- 用粗黑线表示骨骼
- 与受影响区域相关的3-4根骨骼用双线（空心粗线）高亮，标注「候选骨骼集 C(i)」
- 其他骨骼保持普通粗线

【连接关系】：
- 从受影响顶点到候选骨骼之间画几条细虚线，表示权重关联
- 虚线旁标注「w_ik」

右下角添加图例：○ 正常顶点  ● 受影响顶点  ── 普通骨骼  ═ 候选骨骼"""),

    ("fig_1_4_objective_function", """专利技术插图，黑白线条图，白色背景，组件分解图风格。

中心放置一个大的圆角矩形，标注「总目标函数 L(Δw)」

从中心向四周发散出四个分支，每个分支连接一个小矩形框：

【上方分支】：框内标注「拟合误差项」，框下公式「||JΔw - d||²」，旁注「主目标：最小化形变残差」

【右方分支】：框内标注「L2正则项」，框下公式「λ₂||Δw||²」，旁注「防止过拟合」

【下方分支】：框内标注「平滑正则项」，框下公式「λs Σ||Δwi - Δwj||²」，旁注「邻域一致性」

【左方分支】：框内标注「L1稀疏项」，框下公式「λ₁||Δw||₁」，旁注「促进稀疏」

底部约束条件区域用虚线框包围四个约束条件：
「Δw + w₀ ≥ 0」「Σk wik = 1」「|C(i)| ≤ Kmax」「边界连续」"""),

    # ── 专利2 ──
    ("fig_2_1_system_arch", """专利技术插图，黑白线条图，白色背景，系统架构图风格。

上下两层结构：

【上层 - 质量评估框架】：
大矩形框，标题「多维度无真值质量评估框架」

左侧输入：「蒙皮样本」→ 箭头指向框内

框内包含五个并排的小矩形模块（可插拔架构）：
「穿插检测 mpen」「粘连检测 mstick」「共线检测 mcol」「能量异常 meng」「边界连续 mbdry」

五个模块下方汇聚到：「自适应阈值」→「多策略聚合」→「A/B/C分级」

右侧输出三个分支：
- 「A级」→ 绿色标记 →「训练集」
- 「B级」→ 黄色标记 →「降权/人工抽检」
- 「C级」→ 红色标记 →「淘汰/强制修复」

【下层 - 数据闭环】：
大矩形框，标题「数据闭环系统」
包含循环流程：「样本路由」→「困难样本池」→「修复/重训练」→「回归验证」→ 返回「样本路由」

两层之间用双向箭头连接，标注「质量信号反馈」"""),

    ("fig_2_2_pluggable_arch", """专利技术插图，黑白线条图，白色背景，模块化架构图风格。

中央总线式设计：

【顶部输入总线】：一条粗横线，标注「输入数据总线」。上方标注三个输入源：「网格序列」「骨架变换」「蒙皮参数」

【中部独立计算单元】：从总线向下引出五个独立的矩形模块，每个模块内部包含：顶部数据接口、中部计算核心（圆角矩形）、底部输出接口。
五个模块分别标注：「穿插检测器」「粘连检测器」「共线检测器」「能量检测器」「边界检测器」
每个模块旁边有开关符号，表示可启用/禁用

【底部输出总线】：另一条粗横线，标注「指标输出总线」。下方汇聚到「聚合器」模块

右侧有一个虚线框「配置管理器」，用虚线连接到每个检测器的开关"""),

    ("fig_2_3_data_loop", """专利技术插图，黑白线条图，白色背景，循环流程图风格。

环形布局，顺时针方向：

左侧「新样本输入」箭头进入

节点1：矩形框「自动质量评估」，输出三条路径
节点2：菱形判断框「A/B/C分级」
- A → 直接进入「训练数据池」
- B → 进入「人工抽检队列」→ 决策后分流
- C → 进入「困难样本池」

节点3：矩形框「自动/人工修复」，从困难样本池获取输入
节点4：矩形框「回归测试」
节点5：矩形框「阈值/策略调整」，用虚线箭头返回「质量评估」

环形中心放置一个仪表盘图标，标注「质量指标看板」，显示：通过率、异常分布、迭代轮次"""),

    # ── 专利3 ──
    ("fig_3_1_sat_projection", """专利技术插图，黑白线条图，白色背景，几何原理示意图风格。

绘制二维平面上的SAT（分离轴定理）投影演示：

【左侧场景 - 分离状态】：
- 绘制两个简单凸多边形（三角形和四边形），彼此不接触
- 在两者之间绘制一条虚线分离轴（斜向）
- 从分离轴引出垂直投影线，将两个多边形投影到轴上
- 投影区间用粗线段表示，标注「IA」和「IB」
- 两个投影区间之间有间隙，标注「分离间隙 δ」
- 下方标注「分离状态：存在分离轴」

【右侧场景 - 重叠状态】：
- 绘制两个部分重叠的凸多边形
- 绘制一条候选轴
- 投影到轴上后，两个投影区间「IA」和「IB」有重叠部分
- 重叠部分用阴影标记，标注「重叠长度 ω」
- 下方标注「重叠状态：无分离轴」

底部公式：投影区间 I(a) = [min⟨x,a⟩, max⟨x,a⟩]"""),

    ("fig_3_2_detection_objects", """专利技术插图，黑白线条图，白色背景，网格局部放大示意图风格。

左右两部分：

【左侧 - 边链构建】：
- 绘制一小块三角网格（约10-15个三角面）
- 选择其中连续的4-5条边，用粗黑线高亮
- 这些边形成一条链状路径
- 用虚线圈将这条边链圈起来
- 标注「边链对象 Echain」
- 旁边标注应用场景：「用于检测局部折叠、边缘共线」

【右侧 - 面簇构建】：
- 绘制另一小块三角网格
- 选择其中相邻的3-4个三角面，用灰色填充高亮
- 用虚线圈将这组面圈起来
- 标注「面簇对象 Fcluster」
- 旁边标注应用场景：「用于检测局部塌陷、法向翻转」

底部说明：构建原则：基于拓扑邻接关系，自适应扩展范围"""),

    ("fig_3_3_detection_pipeline", """专利技术插图，黑白线条图，白色背景，流程图风格。

从上到下的纵向流程：

输入层：矩形框「输入：网格序列 + 骨架动画」

检测对象构建层：矩形框内两个并排模块「边链构建」「面簇构建」，输出「检测对象集合」

候选轴生成层：矩形框内四个并排小模块「边切向」「面法向」「PCA主方向」「叉积方向」，输出「候选分离轴集」

SAT特征计算层：矩形框「投影区间计算 → 重叠/分离特征提取」，输出三个特征「ωmax」「δmin」「rani」

复合评分层：矩形框内两个并排评分模块「共线性评分 Scol」「形变评分 Sdef」，汇聚到「综合异常分数 S」

时序判定层：矩形框「指数平滑 → 持续帧判定」，输出「异常区域定位 + 触发轴 + 置信度」"""),

    ("fig_3_4_temporal_persistence", """专利技术插图，黑白线条图，白色背景，时序图风格。

上中下三层图表，共享横轴（帧序号 t=0到t=30）：

【上半部分 - 原始信号曲线】：
纵轴：异常分数S。绘制一条波动的折线表示原始异常分数。水平虚线标注阈值「γ2」

【中部 - 平滑后信号曲线】：
同样坐标系，绘制平滑后更平缓的曲线。标注「指数平滑：S̄t = ηSt + (1-η)S̄t-1」

【下半部分 - 持续帧计数器】：
横轴同上。绘制阶梯状计数器曲线。当平滑分数高于阈值时计数器递增。达到K帧时标记「触发告警」。低于阈值时重置为0

右侧图例：实线=原始信号，虚线=平滑信号，水平虚线=阈值线，方块=告警触发点"""),

    # ── 专利4 ──
    ("fig_4_1_dual_phase_framework", """专利技术插图，黑白线条图，白色背景，流程图风格。

从左到右的横向流程：

输入：「网格A」和「网格B」两个输入框，标注「分组约束：检测关系矩阵」

宽相位检测：大矩形框，标题「Broad Phase 宽相位」，内部「BVH层级包围盒」→「快速剔除」→「候选碰撞对」，下方标注「O(n log n) 复杂度」

窄相位检测：大矩形框，标题「Narrow Phase 窄相位」，内部分两路：
路径1（上）：「三角-三角相交测试」→「穿插判定」→ 输出「穿插评分 Pt」
路径2（下）：「点-面距离计算」→「粘连判定」→ 输出「粘连评分 St」

输出：两个评分汇聚到「区域级定位输出」，输出内容：类型、严重度、区域ID、时间范围"""),

    ("fig_4_2_penetration", """专利技术插图，黑白线条图，白色背景，三维几何示意图风格。

绘制两个简化的三维网格局部（如手臂穿过身体的场景）：

主视图：网格A（身体部分）用细线绘制三角网格标注「网格A」，网格B（手臂部分）用粗线绘制三角网格标注「网格B」，两者有明显相交区域

相交区域放大：用虚线框圈出相交部分，相交区域用交叉阴影填充。标注：
- 「穿插深度 d」用双向箭头表示最大穿透距离
- 「穿插面积 a」阴影区域标注面积
- 「穿插面片」高亮显示相交的三角面

底部公式：穿插评分 P = wd·d̂ + wa·â + wc·ĉ（深度 + 面积 + 持续帧）"""),

    ("fig_4_3_sticking", """专利技术插图，黑白线条图，白色背景，几何+运动示意图风格。

绘制两个靠近但不相交的网格局部（如手指贴近手掌的场景）：

【左侧 - 几何特征】：
两个网格表面非常接近，中间有微小间隙。标注「距离 q」两表面间的最小距离。法向量箭头表示两个表面法向。标注「法向一致性」

【右侧 - 运动特征】：
同样的两个网格，添加运动箭头。网格A速度向量「vA」，网格B速度向量「vB」，相对速度「Δv」用虚线箭头。标注「相对速度极低」。切向滑移「slip」用沿表面小箭头

底部：粘连评分 S = αq(1-q̂) + αv(1-v̂) + αs(1-ŝ) + αn·n̂ + αk·k̂"""),

    ("fig_4_4_valid_contact", """专利技术插图，黑白线条图，白色背景，判定流程图风格。

中央主流程：「检测到近距离接触」→ 菱形判断「是否粘连？」

从菱形引出三条抑制规则路径：

规则1 - 白名单豁免（左上分支）：判断框「是否在白名单内？」→ 是 →「使用宽松阈值」→「判定为有效接触」。旁注示例：手指-手掌、脚-地面

规则2 - 滑移释放（右上分支）：判断框「累计滑移 > 阈值？」→ 是 →「重置持续帧计数器」→「判定为有效接触」。旁注：观测窗口内切向位移足够大

规则3 - 运动上下文（下方分支）：判断框「高密度接触场景？」→ 是 →「提高持续帧阈值」→「重新判定」。旁注：抓握、挤压动作

三条规则汇聚到最终判定：「粘连」或「有效接触」"""),

    # ── 专利5 ──
    ("fig_5_1_closed_loop_pipeline", """专利技术插图，黑白线条图，白色背景，闭环流程图风格。

从上到下的纵向流程：

输入：「异常触发信号」+「原始网格/参数」

步骤1 - 异常区域提取：矩形框「异常区域构建」，内部「初始异常点」→「拓扑扩展」→「锚定边界」，输出「修复域 Ω + 边界 ∂Ω」

步骤2 - 目标几何生成：矩形框「目标几何平滑」，内部四项约束「Laplacian平滑」「曲率约束」「边界锚定」「体积保持」，输出「目标位置 y」

步骤3 - 局部映射回写：矩形框「参数映射求解」，内部「几何差值 Δx」→「参数映射 Δθ」→「约束投影」，输出「更新参数 θ'」

步骤4 - 闭环验证：菱形判断框「验证通过？」，四个检验指标：共线下降、穿插下降、曲率下降、边界连续

通过 →「提交更新」→ 输出修复结果
不通过 → 右侧「降级重试」→ 降低平滑强度 → 缩小影响域 → 返回步骤2。重试N次后 →「自动回滚」→「标记失败」"""),

    ("fig_5_2_anomaly_region", """专利技术插图，黑白线条图，白色背景，网格区域标注示意图风格。

绘制一块布料网格的俯视图（约20×20网格点规模）：

【中心 - 初始异常区域】：几个相邻顶点标记为实心黑点，用深灰色填充这些点组成的三角面。标注「初始异常点集 Va」，旁注「A(v) ≥ τa」

【中层 - 扩展区域】：在初始区域外围扩展1-2圈顶点，用空心圆圈标记，浅灰色填充。标注「拓扑扩展域」，旁注「自适应半径 re」

【外层 - 锚定边界】：扩展区域最外层顶点用双圈符号标记，虚线连接形成边界线。标注「锚定边界 ∂Ω」，旁注「边界锚定强约束」

【外部】：正常网格用最细线条绘制，标注「不受影响区域」

图例：● 初始异常点  ○ 扩展区域点  ⊙ 锚定边界点"""),

    ("fig_5_3_smoothing_comparison", """专利技术插图，黑白线条图，白色背景，前后对比示意图风格。

左右对比布局：

【左侧 - 平滑前】：绘制一块布料网格的侧视剖面。表面有明显的局部异常：尖锐褶皱（锯齿状曲线）、局部凹陷、不自然的曲率突变。用虚线圈出问题区域。标注「原始状态 x」，旁注问题：共线、曲率异常、抖动

【中间 - 处理过程】：箭头连接左右两侧，箭头上标注：Laplacian平滑、曲率约束、边界锚定、体积保持

【右侧 - 平滑后】：同样位置的布料网格侧视剖面。表面变得平滑自然，褶皱柔和、凹陷填平、曲率过渡自然。边界位置与左侧对齐。标注「目标状态 y」，旁注：平滑、自然、边界保持"""),

    ("fig_5_4_rollback_decision_tree", """专利技术插图，黑白线条图，白色背景，决策树流程图风格。

从上到下的树状决策结构：

根节点：椭圆形「首次后处理执行」

判断节点1：菱形「验证通过？」
- 是 → 矩形「提交成功」→ 结束
- 否 → 继续

重试节点1：矩形「降级策略1：降低平滑强度 λl」

判断节点2：菱形「验证通过？」
- 是 → 矩形「提交成功」→ 结束
- 否 → 继续

重试节点2：矩形「降级策略2：进一步降低强度」

判断节点3：菱形「验证通过？」
- 是 → 矩形「提交成功」→ 结束
- 否 → 继续

重试节点3：矩形「降级策略3：缩小影响域」

判断节点4：菱形「验证通过？」
- 是 → 矩形「提交成功」→ 结束
- 否 → 矩形「自动回滚到原始状态」→「标记后处理失败」→ 结束

旁注：最大重试次数 Nretry，每次重试记录日志"""),
]

total = len(FIGURES)
print(f"共需生成 {total} 张图片（图1-1已生成，从图1-2开始）")
print("=" * 60)

results = []
for i, (prefix, prompt) in enumerate(FIGURES):
    print(f"\n[{i+1}/{total}] 生成: {prefix}")
    try:
        paths = generate_images_with_remote_api(
            prompt=prompt,
            output_dir=OUTPUT_DIR,
            output_name_prefix=prefix,
        )
        results.append((prefix, True, paths))
        print(f"  OK: {paths}")
    except Exception as e:
        results.append((prefix, False, str(e)))
        print(f"  FAIL: {e}")
    # 避免 API 限流
    time.sleep(2)

print("\n" + "=" * 60)
print("生成结果汇总：")
ok = sum(1 for _, s, _ in results if s)
print(f"成功: {ok}/{total}  (加上已有的 fig_1_1 共 {ok+1}/17)")
for prefix, success, info in results:
    status = "OK" if success else "FAIL"
    print(f"  [{status}] {prefix}")
